---
colly:
  image: ghcr.io/netcracker/qubership-colly:1.8.0
  serviceName: qubership-colly
  instancesRepo:
  cronSchedule: 0 0/20 * * * ?
  idp:
    url:
    clientId: colly
    clientSecret:
  db:
    host:
    username: postgres
    password: postgres
  ports:
    http: 8080
  ingress:
    http:
      annotations:
